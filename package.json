{
  "name": "claude-code-reset",
  "version": "0.1.0",
  "description": "Local, backup-first CLI to inspect and reset Claude Code local state (credentials, account/billing cache, telemetry, auto-backups) on Linux/macOS.",
  "type": "commonjs",
  "bin": {
    "ccreset": "bin/ccreset.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "files": [
    "bin",
    "lib",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test"
  },
  "keywords": [
    "claude-code",
    "cli",
    "cleanup",
    "reset",
    "local-data",
    "privacy"
  ]
}
